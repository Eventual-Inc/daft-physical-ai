# 06 - Writing data

Persist curated and annotated data in training-ready layouts.

Planned: `write_rollout_parquet.py` (the canonical one-row-per-step contract
via `daft_physical_ai.episodes`), LeRobot/Zarr writers as Daft support lands.
