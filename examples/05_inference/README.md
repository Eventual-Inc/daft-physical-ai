# 05 - Inference

Model-backed labeling through Daft's AI functions (`prompt`, `embed_image`,
`embed_text`) - no provider plumbing in this repo.

Planned: `structured_episode_label.py` (prompt + Pydantic label schema),
`classify_failure_with_frame.py` (VLM over a contact sheet),
`embed_and_search_frames.py` (semantic search over robot video frames).
