# 07 - Training handoff

Hand a curated Daft dataframe to a training loop without an export detour.

Planned: `curated_dataset_to_torch_dataloader.py` (filter/join/label in Daft,
then `to_torch_dataloader` straight into a training step).
